# Design-iteration screenshots. Keep them out of the repo: they are large,
# they are never served, and nothing links to them. Put them in an issue or
# a gist if they are worth keeping.
screenshots/
*.png

# Editor and OS noise
.DS_Store
